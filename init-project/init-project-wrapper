#!/bin/bash

# Wrapper script for init-project (Linux/macOS)
# Allows: init-project --start, init-project --stop, etc.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the actual script with arguments
bash "$SCRIPT_DIR/init-project.sh" "$@"
